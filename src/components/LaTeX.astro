---
import { themeConfig } from '~/.config'

const { latex } = themeConfig

const katex = latex.katex
---

{
  katex && (
    <link
      rel="stylesheet"
      href="https://jsd.onmicrosoft.cn/npm/katex@latest/dist/katex.min.css"
      crossorigin="anonymous"
    />
  )
}
